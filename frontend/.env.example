# Backend API URL for production deployment
# Leave empty for local dev (Vite proxy handles /api → localhost:5000)
# For Vercel: set to your deployed backend URL, e.g. https://your-backend.vercel.app/api
VITE_API_URL=
